import React from 'react';
import styles from './SloganTitel.module.css';

function Title() {
    return (
        <div className={styles.titleContainer}>
            <h2 className={styles.title}>I consider options for remote work</h2>
        </div>
    );
}

export default Title;
